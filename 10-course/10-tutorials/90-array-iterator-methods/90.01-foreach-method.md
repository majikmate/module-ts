# ForEach Method

```typescript
// forEach() executes a function for each array element
// Unlike map/filter, it doesn't return a new array

const numbers = [1, 2, 3, 4, 5];

// Print each number
numbers.forEach((num) => {
    console.log("Number:", num);
});

// forEach with index parameter
const names = ["Alice", "Bob", "Charlie"];
names.forEach((name, index) => {
    console.log(`${index + 1}. ${name}`);
});
// Output:
// 1. Alice
// 2. Bob
// 3. Charlie

// Use forEach for side effects (e.g., updating external variables)
let sum = 0;
numbers.forEach((num) => {
    sum = sum + num;
});
console.log("Sum:", sum); // 15

// Note: forEach doesn't create a new array
const result = numbers.forEach((num) => num * 2);
console.log("Result:", result); // undefined
```

📺 Klicke auf das Bild, um das Video-Tutorial anzusehen:

_Video wird noch hinzugefügt._

---

_This project is licensed under the [CC BY-SA 4.0](../../../LICENSE)._
