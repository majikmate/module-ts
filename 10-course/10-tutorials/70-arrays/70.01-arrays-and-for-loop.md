# Arrays & for loop

```typescript
// calculate the sum of the values 1, 2, 3, 4, ...

// declare the array
const numbers = [1, 2, 3, 4];

// add additional values to the numbers array
numbers.push(17);
numbers.push(22);

// delcare the sum variable
let sumOfNumbers = 0;

// iterate over all array elements and calculate the sum
for (let i = 0; i < numbers.length; i = i + 1) {
    sumOfNumbers = sumOfNumbers + numbers[i];
}

// print the sum of numbers to the console
console.log(`The sum of the numbers ${numbers} is ${sumOfNumbers}`);
```

📺 Klicke auf das Bild, um das Video-Tutorial anzusehen:

[![Watch on YouTube](https://img.youtube.com/vi/WQDKnj9k2Ys/0.jpg)](https://www.youtube.com/watch?v=WQDKnj9k2Ys)

---

_This project is licensed under the [CC BY-SA 4.0](../../../LICENSE)._
