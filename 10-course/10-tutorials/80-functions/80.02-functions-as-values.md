# Functions as Values

```typescript
// Functions are first-class citizens in TypeScript/JavaScript
// They can be assigned to variables

function multiply(a: number, b: number): number {
    return a * b;
}

// Assign function to a variable
const operation = multiply;

// Call the function through the variable
const result = operation(4, 5);
console.log("Result:", result);

// Function expression - define and assign at once
const divide = function (a: number, b: number): number {
    return a / b;
};

console.log("Division:", divide(10, 2));
```

📺 Klicke auf das Bild, um das Video-Tutorial anzusehen:

_Video wird noch hinzugefügt._

---

_This project is licensed under the [CC BY-SA 4.0](../../../LICENSE)._
